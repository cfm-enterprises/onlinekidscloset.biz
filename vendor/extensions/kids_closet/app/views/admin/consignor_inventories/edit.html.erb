<% render_region :main do |main| %>
  <% main.edit_form do %>
    <% form_for [:admin, @sale, @consignor_inventory], :html => {:class => 'semantic-form', :multipart => true, :name => "consignor_inventory_form"} do |f| %>
      <%= render :partial => 'form', :locals => { :f => f, :save_label => 'Update' } %>
    <% end %>
  <% end %>
<% end %>